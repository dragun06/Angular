{
  "dependencies": {
    "express": "^4.18.2",
    "mongodb": "^6.3.0"
  },
  "scripts": {
    "ng": "ng",
    "start": "cd api && npm install && node server.js",
    "build": "cd assignment-app && npm install && ng build && node server.js",
    "watch": "ng build --watch --configuration development",
    "test": "ng test"
  }
}
